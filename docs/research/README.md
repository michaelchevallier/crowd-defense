# `docs/research/`

Rapports de recherche industrie + audit produits pendant les sprints R1-R3 du plan stratégique Phaser (archive). Ramenés ici comme **référence stratégique stable** pour la migration Unity — les findings et benchmarks restent moteur-agnostiques.

Format de nommage : `R<sprint>-<NN>-<axis>-benchmark.md` ou `R<sprint>-<NN>-<axis>-audit.md`.

Contenu :
- **R1** : R1-01 (économie), R1-02 (pacing), R1-03 (mapdesign), R1-04 (auto-QA AI patterns)
- **R2** : R2-04 (synergies), R2-05 (difficulty curve), R2-06 (Milan current audit), R2-07 (perf baseline)
- **R3** : R3-01 (tooling research — DÉPRÉCIÉ par pivot Unity, garder pour pattern Inspector custom), R3-02 (portability research — a abouti au pivot Unity Q18=B)

**Référence plan historique Phaser** (archive — pivot Unity acté 2026-05-11) : `/Users/mike/.claude/plans/rustling-nibbling-wirth.md` §5.

**À ne pas utiliser comme source pour la migration Unity** — ces rapports servent de référence stratégique stable (specs design moteur-agnostiques), pas de plan d'exécution. Le plan Unity actuel est `STATUS.md` racine + (à créer) `/plan` formel Phase 1+.
