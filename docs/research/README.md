# `.claude/research/`

Rapports de recherche industrie produits par l'agent `td-researcher` (et `qa-tester`/`perf-auditor` pour audit interne) pendant les sprints R1 et R2 du plan stratégique 8 semaines.

Format de nommage : `R<sprint>-<NN>-<axis>-benchmark.md` ou `R<sprint>-<NN>-<axis>-audit.md`.

Sprints qui produisent ici :
- **R1** : R1-01 (économie), R1-02 (pacing), R1-03 (mapdesign), R1-04 (auto-QA AI patterns).
- **R2** : R2-04 (synergies), R2-05 (difficulty curve), R2-06 (Milan current audit), R2-07 (perf baseline).

Référence plan : `/Users/mike/.claude/plans/rustling-nibbling-wirth.md` §5.
