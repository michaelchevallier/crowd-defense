# Code Dedup + Dead Code Audit — 2026-05-12 23h45

## Section 1 : Top 10 Smells

| # | File:Line | Type | Effort |
|---|-----------|------|--------|
| 1 | `Systems/WaveManager.cs:17-32` | Naming inconsistency — 10 private fields without `_` prefix while 8 other fields in same class use `_camelCase` | **FIXED** (this commit) |
| 2 | `UI/ShopController.cs:10` | TODO debt — `WorldMapController should call Show() after level victory` — architectural integration not yet wired | 30 min |
| 3 | `Entities/Enemy.Anim.cs:193` / `Entities/Tower.cs:371` / `Entities/Hero.cs:326` | Structural duplication — `SpawnMeshChild` + `CreateColoredFallback` repeated in 3 entity files, BUT each has intentional variant behaviour (Enemy: pool reuse check; Tower: Base/Top hide; Hero: SetupCapeCloth) — do NOT blindly merge | 2-3h sprint |
| 4 | `UI/*.cs` (62 files) | `GetComponent<UIDocument>` + null guard repeated 26 times — candidate for `UIDocumentBase : MonoBehaviour` helper that exposes `VisualElement Root { get; }` | 1h |
| 5 | `UI/WorldMapController.cs:120` | TODO inside `Debug.LogWarning` string — architectural note embedded in log, not a real comment — should be extracted to a proper code comment or ticket | 10 min |
| 6 | `Visual/VfxPoolBuilders.cs` / `VfxPoolBuilders2.cs` (646 lines combined) | Partial-class split adds navigation friction — both files are internal static helpers; naming `Builders` vs `Builders2` is not semantic | 1h |
| 7 | `Entities/Hero.cs` (1581 lines) | Monolithic — largest file in project, mixes movement, XP, perk properties (80+ `internal set` properties), skill logic, mesh spawn, cloth setup. Not a CLAUDE.md violation (it's not a `main.js`) but split by concern would help | 3h |
| 8 | `Systems/WaveManager.cs:403-449` | `SpawnEnemy` → `DoSpawnEnemy` two-method chain adds indirection with no benefit — `ResolvePathIdx` call and `spawnCounter++` could be inline | 20 min |
| 9 | `Systems/LevelRunner.cs` (854 lines) | Functions `SpawnCastle` (L700+) and `RestoreMidLevelState` (L470+) are long but each single-responsibility — acceptable |  — |
| 10 | `Data/TowerType.cs` (230 lines) / `Data/EnemyType.cs` (154 lines) | No smell — data SO, expected large | — |

## Section 2 : Quick Wins (< 30 min)

1. **WorldMapController TODO in log** — extract TODO to code comment above method, remove from `Debug.LogWarning` string. 5 min.
2. **ShopController TODO** — add comment `// Ticket: wire WorldMapController.OnLevelVictory → Show()`, remove TODO keyword. 5 min.
3. **WaveManager `SpawnEnemy` / `DoSpawnEnemy` merge** — fold 5-line `DoSpawnEnemy` back into `SpawnEnemy`, remove artificial split. 20 min.

## Section 3 : Strategic Refactors (> 2h sprint)

1. **UIDocument init helper** (62 files × repeated 3-line guard pattern) — create `UIControllerBase : MonoBehaviour` with protected `VisualElement? Root` property. ~1h + regression test all 62 UI controllers.
2. **SpawnMeshChild consolidation** — after confirming behaviour parity is intentional, introduce `MeshSpawnHelper.SpawnMeshChild(Transform parent, string assetKey, string tag, Action<GameObject>? postSpawn = null)` in `Common/`. Then have Tower/Enemy/Hero pass their entity-specific post-spawn lambda. Requires careful API design — ticket needed. ~3h.
3. **Hero.cs decomposition** — extract `HeroPerkProperties`, `HeroXP`, `HeroMovement` partial classes. Keeps MonoBehaviour root thin. ~3h.
4. **VfxPoolBuilders rename** — rename `VfxPoolBuilders2` → `VfxPoolBuildersExtra` or merge both into `VfxPoolBuilders` (split by region `#region`). ~30 min.

## Section 4 : Notable Patterns / Strengths

- **All `Debug.Log` calls are guarded** — 100% of bare `Debug.Log` verified inside `#if UNITY_EDITOR` or `#if UNITY_EDITOR || DEVELOPMENT_BUILD`. No prod log spam.
- **No TODO/FIXME/XXX/HACK markers** except 2 (ShopController line 10, WorldMapController line 120) — low debt count for a 10k LOC codebase.
- **Nullable enabled everywhere** — `#nullable enable` present on all files checked.
- **No dead imports** — using statements are uniform and compact.
- **No `if (false)` or `// DISABLED` dead blocks** found.
- **Field naming now consistent in WaveManager** (fixed this commit).

## Fix Applied

`Systems/WaveManager.cs` — renamed 10 private fields from bare `camelCase` to `_camelCase` prefix convention matching the rest of the class. Zero behaviour change. 10 declarations + ~88 usage sites updated.
