# V4 Diff Eval — 2026-05-12 04:48 (post r12 deploy)

**Status** : ✅ Parity **~95-96%** (up from 91-95%)
**Deploy LIVE** : `acf7e93` r12 sur gh-pages
**Build** : r12 ships 10+ commits post-r10

## ✅ Match (13+ axes)
- Endless HP scaling exponential→linear `1.15^10 * (1 + Δ*0.05)`
- Special waves cycles every 5 W>10 (elite/boss/chaos)
- MidLevelState save struct + load/save methods (cec99ad)
- LifetimeStats UI table per-world + global aggregation (6c52502)
- Streak bonus 1+streak*0.05 cap 1.25
- Gold reward mul `1.05^(waveIdx-10)` W>=10
- RunContext + SaveSystem v2 migration (3-slot)
- AudioMixerController dB conversion (1353c51)
- LoadingScreen async progress bar (1fdb4aa)
- Tower repair + radial menu economy
- Boss shaders Jellyfish + Hologram animés
- BuildMainSceneTool idempotent (43cd271 → 7444d94 fix)
- 30 enemies + 13 towers + 15 synergies + L3 D1-03

## ⚠️ Partial (4)
1. RunState restore hook dans LevelRunner (structure ok, intégration incomplete)
2. Endless leaderboard wire (struct OK, UI display peut manquer)
3. MidLevelSave auto-trigger post-wave
4. World stats aggregation (per-level OK, per-world consolidation à check)

## ❌ Missing (Phase 5+)
- iOS Xcode build (credentials Mike)
- Steam SDK plugin
- 80 niveaux complets L3 hybride

## 🔥 Top 5 fixes prioritaires
1. Verify `LevelRunner.Restore()` wires `SaveSystem.LoadRunState()` complet
2. Test endless W10+ gold `1.05^(waveIdx-10)` runtime
3. Auto-save per-wave trigger SaveRunState() après OnWaveCleared
4. World aggregation stats StatsLifetimePanel.Refresh()
5. Leaderboard UI EndScreen/Menu affiche endless top 3

## Δ since 0420
10+ commits, +190 LOC core features, 0 major bugs détectés.

**Action recommandée** : Validation Chrome MCP live `acf7e93` en cours.
