# V4 Diff Eval — 2026-05-12 05:20

**Status** : ✅ **~94-95% parity** — stable, polishing phase
**Deploy LIVE** : `7b56bda` (r16)
**HEAD main** : 6 commits post-r16 deploy

## ✅ Match (15+ axes V4 parity)

1. L3 hybrid branch choice (UpgradeMenuController modal)
2. Mid-level auto-save per-wave (LevelRunner.SnapshotMidLevel → SaveRunState)
3. Mid-level resume (RestoreMidLevelStateIfPending + LoadRunState)
4. Endless gold mul `1.05^(waveIdx-10)` W≥10
5. Achievement counter persistence (PlayerPrefs)
6. Leaderboard logging (AddLeaderboardEntry + GetTopScores 10)
7. Per-world stats (StatsLifetimePanel.PopulateRows)
8-15. Endless HP scaling, streak cap 1.25, save v3, audio dB, async loader, tower repair, boss shaders, 30 enemies, 13 towers, 15 synergies

## ⚠️ Partial (2)

1. Leaderboard UI EndScreen — struct wired, visual rendering needs live verify
2. World aggregation edge cases — logic solid, confirmation pending

## ❌ Missing (Phase 5+, out-of-scope)

- iOS Xcode build
- Steam SDK plugin
- 80 L3 hybrid levels content

## 🔥 Top 5 fixes prioritaires (live QA needed)

1. Live test leaderboard UI EndScreen → top 3 entries render
2. Live test mid-level resume: pause → reload → restore gold/towers/hero intact
3. Live test endless W12+ gold mul visible vs W10 baseline
4. Live test per-world stats StatsLifetimePanel rows all 10 worlds
5. Polish EndlessLeaderboardUI if not yet wired

## Δ since 0448

6 commits :
- `acdb87a` ToastQueue cap
- `c92c75f` PerkPicker shuffle
- `f3fed19` PathManager cache
- `ad83d05` Placement haptic
- `dd04ca2` PathTiles bridge sharedMaterial
- `83fed46` Hero levelup celebration

**Parity hold** : 94-95% — ready for iOS cert prep ~2026-05-15.
